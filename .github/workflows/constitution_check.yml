name: Covenant Integrity Check

on:
  pull_request:

jobs:
  covenant-guard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Detect covenant modifications
        run: |
          git fetch origin main
          git diff --name-only origin/main...HEAD | grep "^covenant/" && exit 1 || exit 0
